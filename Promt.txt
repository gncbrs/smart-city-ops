Task: Document Phase 5.34 (In-Flight Field Unit Dynamic Position Resolution in Recommendation Engine) in docs/DEVELOPMENT_LOG.md and CLAUDE.md.

Context:
In Phase 5.34, we fixed the in-flight destination illusion where the recommendation engine previously evaluated travelling field units at their destination coordinates rather than their interpolated real-time position along the route.

Requirements:
1. Update `docs/DEVELOPMENT_LOG.md`:
   - At the end of Part 12, add section `## 59. Phase 5.34 — In-Flight Field Unit Dynamic Position Resolution in Recommendation Engine`.
   - Document the motivation (preventing travelling/dispatched units from being scored as if they had already arrived at their active task's destination, ensuring true distance/ETA scoring across concurrent operations).
   - Detail the architecture and implementation:
     - Added `GeoCalculator.GetInFlightPosition` in `SmartCityOps.Application.Common` to compute interpolated coordinates based on origin, destination, assigned time, and estimated travel duration.
     - Updated `FieldUnitRecommendationService` to resolve effective real-time positions for all in-flight units before issuing batch OSRM Table or Haversine fallback queries.
   - Confirm verification results: `dotnet build SmartCityOps.sln` clean (0 warnings, 0 errors).

2. Update `CLAUDE.md`:
   - In the Architecture / Backend section, note `GeoCalculator.GetInFlightPosition` and in-flight position resolution in `FieldUnitRecommendationService`.
   - In the "Current status" section, append a concise summary paragraph for Phase 5.34 referencing `docs/DEVELOPMENT_LOG.md` Part 12 §59.

3. Verification:
   - Ensure clean markdown hierarchy and preserved document anchors.