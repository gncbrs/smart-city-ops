Update `docs/DEVELOPMENT_LOG.md` and `docs/To-Do-List.txt` to record the FilterPanel Filter Chip modernization.

### Tasks & Steps:
1. **Update `docs/DEVELOPMENT_LOG.md`:**
   - Under `## 48. Phase 5.23 — Sidebar Active Incidents List, Priority Score Bars & Statistics Reorganization` (or as a Step 4 note):
     - Document modernizing `FilterPanel` / `FilterCheckboxGroup` by replacing native checkboxes with compact, accessible Filter Chip / Pill buttons.
     - Document chip variants: default blue (`#2563EB`) for Status/Type and semantic priority badges (`High: #EF4444`, `Medium: #D97706`, `Low: #2563EB`) with active dot indicators.
     - Note that compact sizing (`padding: 4px 10px`, `font-size: 0.75rem`, `border-radius: 6px`, `gap: 6px`) and `flex-shrink: 0` keep the panel fixed and 100% scrollbar-free.

2. **Update `docs/To-Do-List.txt`:**
   - Add a note under completed archive `[x]` stating that `FilterPanel` native checkboxes were modernized into compact filter chips with priority color variants — Phase 5.23, §48.

### Verification:
Ensure `npm run lint` and `npm run build` in `frontend/` remain completely green.