Task: Decompose RestrictedZonesSection.tsx - Step 3/3 (Final Composition & Cleanup)

### Objective
Finalize the decomposition of `frontend/src/features/restricted-zones/components/RestrictedZonesSection.tsx` by replacing the internal inline state and rendering logic with the hooks and subcomponents created in Steps 1 & 2.

### Requirements
1. Refactor `frontend/src/features/restricted-zones/components/RestrictedZonesSection.tsx`:
   - Consume `useRestrictedZoneForm(pickedCoordinates, onCoordinatesApplied)`.
   - Consume `useRestrictedZoneEdit()`.
   - Render `RestrictedZoneTable` and `RestrictedZoneForm` with their respective props and handlers.
   - Maintain the existing component signature, props interface (`RestrictedZonesSectionProps`), and styling wrappers (`.restricted-zones-section`).
   - Reduce the component size down to a clean composition container (~40-60 lines).

2. Verification:
   - Run `npm run lint` and `npm run build` in `frontend/` to confirm that the entire feature builds cleanly with zero errors.

3. Documentation:
   - Update `docs/DEVELOPMENT_LOG.md` under Part 12 (as section 35) and update `CLAUDE.md`.
   - In `docs/To-Do-List.txt`, mark the item `RestrictedZonesSection.tsx bileşenini parçala` as completed `[x]`.