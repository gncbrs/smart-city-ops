Task: Document Phase 5.26 (OSRM Real Road-Network Navigation & Route Geometry Integration) in docs/DEVELOPMENT_LOG.md and CLAUDE.md.

Context:
In Phase 5.26, we upgraded the field unit dispatch movement from Euclidean straight-line interpolation to real road-network navigation using OSRM (Open Source Routing Machine). The backend queries OSRM for driving routes, stores the GeoJSON polyline coordinates in `OperationalTask.RouteGeometry`, and the frontend traverses this exact road geometry over time while rendering the road route on the map.

Requirements:
1. Update `docs/DEVELOPMENT_LOG.md`:
   - At the end of Part 12, add section `## 51. Phase 5.26 — OSRM Real Road-Network Navigation & Route Geometry Integration`.
   - Document the motivation (moving from straight-line visual approximation to genuine road-following vehicle navigation across Ankara's street grid).
   - Detail the architecture and implementation steps:
     - Step 1.1: `IRoutingService` (`RouteGeometryResult`) in Application layer, `OsrmRoutingService` in Infrastructure layer calling `https://router.project-osrm.org/route/v1/driving/...` with CultureInfo.InvariantCulture formatting, User-Agent header, 3s timeout, and Haversine 40 km/h fallback, registered via `AddHttpClient`.
     - Step 1.2: Added `RouteGeometry` (`string?`) to `OperationalTask` entity/DTO and `OperationsReplayService` snapshot, integrated `IRoutingService` into `OperationalTaskService.AssignFieldUnitAsync` (retrieving route and real duration on dispatch/reassign, deprecating inline `IEtaEstimator` usage in this method), generated and applied EF Core migration `AddOperationalTaskRouteGeometry`.
     - Step 1.3: Updated frontend types (`OperationalTask.routeGeometry`), enhanced `geoInterpolation.ts` (`getCurrentPosition`, `interpolateAlongPolyline`, `haversineDistance`) to interpolate along multi-point polyline segments matching travel progress, updated `useDispatchedRouteLayers.ts` to render the exact GeoJSON road line.
     - Live Verification & Bug Fix: Addressed Turkish locale comma formatting and OSRM User-Agent requirements, verified via live assignment returning ~330 coordinate points along real streets.
   - Note verification results: `dotnet build SmartCityOps.sln` clean (0 warnings, 0 errors), `npm run lint` and `npm run build` clean (0 errors), DB migration applied.

2. Update `CLAUDE.md`:
   - In the Architecture / Backend section, note that task creation now fetches real driving routes via `IRoutingService` (OSRM).
   - In the "Current status" section, add a summary paragraph for Phase 5.26 referencing `docs/DEVELOPMENT_LOG.md` Part 12 §51.

3. Verification:
   - Ensure clean markdown hierarchy, valid formatting, and preserved document anchors.