Please implement Step 8.4 of Madde 8: Update documentation in `docs/DEVELOPMENT_LOG.md` and `CLAUDE.md`.

Required Changes:
1. Open `docs/DEVELOPMENT_LOG.md`:
   - Append a new section under Part 12: `## 69. Phase 5.44 — Single-Resource GET /{id} REST Endpoints Support (Madde 8)`.
   - Document:
     * Problem: Controllers only exposed bulk/collection endpoints (e.g. `GET /api/incidents`, `GET /api/field-units`). Single-resource retrieval (`GET /{id}`) was missing across all core entities, limiting external integrations and REST maturity.
     * Solution: Added `GetByIdAsync` to application service interfaces (`IIncidentService`, `IFieldUnitService`, `IOperationalTaskService`, `IRestrictedZoneService`) and implemented them in infrastructure services returning DTO or `null`. Added `[HttpGet("{id:guid}")]` endpoints across all 4 controllers returning `200 OK` or `404 NotFound()`. Updated `SmartCityOps.Api.http` test definitions.
     * Verification: `dotnet build SmartCityOps.sln` verified clean (0 warnings, 0 errors).
   - Update the Table of Contents at the top of `docs/DEVELOPMENT_LOG.md` to include Section 69.
2. Open `CLAUDE.md`:
   - In "Architecture" -> Backend controller endpoint descriptions, note the presence of `GET /{id}` on Incidents, FieldUnits, Tasks, and RestrictedZones.
   - In "Current status", add a brief summary of Phase 5.44 and remove the "missing single-resource GET /{id} endpoints" entry from the open issues list.

Please apply the documentation updates and verify with `dotnet build Src/SmartCityOps.sln`.