Please update `docs/DEVELOPMENT_LOG.md` and `CLAUDE.md` to document the changes completed in Madde 1 and Madde 2 (Phase 5.36).

Required Changes:
1. Open `docs/DEVELOPMENT_LOG.md`:
   - Append a new section under Part 12: `## 61. Phase 5.36 — FieldUnit Movement History Query Projection Safety & DomainConflictException Architecture`.
   - Document:
     * Step 1.1: `FieldUnitService.GetMovementHistoryAsync` refactored to materialize raw enum values from PostgreSQL first via `ToListAsync()`, performing `.ToString()` projection in-memory to prevent EF Core/Npgsql SQL translation failures.
     * Step 2.1 & 2.2: Creation of `DomainConflictException` in `SmartCityOps.Domain.Exceptions`, refactoring `IncidentService.ResolveAsync` and `FieldUnitService.UpdateStatusAsync` to throw `DomainConflictException` instead of generic `InvalidOperationException` for business-rule conflicts, and updating `DomainExceptionHandler` to map `DomainConflictException` to 409 Conflict while removing the broad `InvalidOperationException` -> 409 mapping.
   - Update the Table of Contents at the top of `docs/DEVELOPMENT_LOG.md` to include Section 61.
2. Open `CLAUDE.md`:
   - In "Architecture" -> `SmartCityOps.Api`, update the `DomainExceptionHandler` description to reflect `DomainConflictException` mapping and the removal of `InvalidOperationException`.
   - In "Current status & known open issues", add a brief summary of Phase 5.36.

Please provide the diff and verify with `dotnet build Src/SmartCityOps.sln`.