Task: Stabilize Field Unit Markers Animation Loop (Performance Refactor)

### Objective
Ensure the continuous animation loop in `frontend/src/features/operations-map/hooks/useFieldUnitMarkers.ts` runs seamlessly without getting torn down and restarted on every SignalR update or data refetch.

### Requirements
1. In `frontend/src/features/operations-map/hooks/useFieldUnitMarkers.ts`:
   - Keep `operationalTasks` and `fieldUnits` updated in refs:
     - Ensure `fieldUnitsByIdRef.current` (or an `operationalTasksRef`) always contains the latest references on every render.
   - Refactor the continuous animation `useEffect`:
     - Change its dependency array from `[map, fieldUnits, operationalTasks]` to `[map]`.
     - The inner `tick` function should read `operationalTasksRef.current` and `fieldUnitsByIdRef.current` (or iterate over `markersRef.current` and look up units/tasks from refs) instead of closing over reactive array props.
     - Ensure the loop starts when `map` is ready and only stops (cancels `animationFrameId`) when `map` unmounts.
   - Ensure the separate marker diffing / creation / removal `useEffect` continues to update marker DOM instances when `fieldUnits` change.

2. Verification:
   - Run `npm run lint` and `npm run build` in `frontend/` to confirm zero lint and zero TypeScript errors.

3. Documentation:
   - Update `docs/DEVELOPMENT_LOG.md` under Part 12 (as section 39).
   - In `docs/To-Do-List.txt`, mark the item `useFieldUnitMarkers.ts animasyon döngüsünü sürekli çalışır hale getir` as completed `[x]`.