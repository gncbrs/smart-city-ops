Please refactor `OperationsReplayService.GetReplayTimeRangeAsync` in `Src/SmartCityOps.Infrastructure/OperationsReplay/OperationsReplayService.cs` to optimize the time range calculation:

### Tasks:
1. Review lines 92-106 in `OperationsReplayService.cs`. Currently, it executes 8 sequential scalar queries (`MinAsync`/`MaxAsync` on `Incidents.ReportedAt`/`ResolvedAt`, `FieldUnitLocationHistories.RecordedAt`, `OperationalTasks.AssignedAt`/`CompletedAt`).
2. Streamline / consolidate these queries into optimized database round-trips (or a single consolidated aggregate query if feasible with EF Core Npgsql provider) to compute the global `MinTimestamp` and `MaxTimestamp`.
3. Verify that `dotnet build` from `Src/` passes cleanly with 0 errors.

---

### Mandatory Documentation Update:
- Update `docs/DEVELOPMENT_LOG.md` (Part 12):
  - Add a new section `## 23. Phase 5.9 — OperationsReplay Time Range Query Optimization`.
  - Document the consolidation of the sequential scalar Min/Max queries in `OperationsReplayService`.
  - Update the Table of Contents and the summary table (renumbering conclusion to `## 24. Sonuç ve sıradaki adım`).
- Update `CLAUDE.md` if any references need sync.

Please proceed with this refactoring step.