Please update the documentation files (`docs/DEVELOPMENT_LOG.md` and `CLAUDE.md`) to reflect the selection toggle and deselect fixes we just completed:

### Updates to perform:
1. **`docs/DEVELOPMENT_LOG.md` (Part 12):**
   - Add a new sub-section (e.g. `## 16. Phase 5.2 — Selection Toggle, Empty Map Deselect & Event Bubbling Fix` or similar).
   - Document:
     - Toggle on re-clicking already selected markers (`useSelection.ts`).
     - Click on empty map canvas space to trigger `clearSelection` (`useMapInstance.ts`).
     - The event bubbling issue (`event.stopPropagation()`) where marker clicks bubbled to map canvas causing instant deselection.
     - The panel close (`✕`) buttons in `IncidentPanel` and `FieldUnitPanel`.
   - Update the Table of Contents at the top and the summary table at the end of the log.
2. **`CLAUDE.md`:**
   - Update the "Current status & known open issues" section to note that selection deselect/toggle behavior is now fully resolved and event propagation is handled.

Please apply these updates now.